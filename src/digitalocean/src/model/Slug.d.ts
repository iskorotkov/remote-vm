/**
 * The Slug model module.
 * @module model/Slug
 * @version 2.0
 */
export class Slug {
    /**
     * Constructs a <code>Slug</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Slug} obj Optional instance to populate.
     * @return {module:model/Slug} The populated <code>Slug</code> instance.
     */
    static constructFromObject(data: any, obj: any): any;
}
