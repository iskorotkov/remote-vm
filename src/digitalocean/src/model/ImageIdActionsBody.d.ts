/**
 * The ImageIdActionsBody model module.
 * @module model/ImageIdActionsBody
 * @version 2.0
 */
export class ImageIdActionsBody {
    /**
     * Constructs a <code>ImageIdActionsBody</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ImageIdActionsBody} obj Optional instance to populate.
     * @return {module:model/ImageIdActionsBody} The populated <code>ImageIdActionsBody</code> instance.
     */
    static constructFromObject(data: any, obj: any): any;
}
