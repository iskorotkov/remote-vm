/**
 * The ProjectsProjectIdBody model module.
 * @module model/ProjectsProjectIdBody
 * @version 2.0
 */
export class ProjectsProjectIdBody extends Project {
    /**
     * Constructs a new <code>ProjectsProjectIdBody</code>.
     * @alias module:model/ProjectsProjectIdBody
     * @class
     * @extends module:model/Project
     * @param isDefault {} If true, all resources will be added to this project if no project is specified.
     */
    constructor(isDefault: any);
}
import { Project } from "./Project";
