/**
 * The DropletIdActionsBody model module.
 * @module model/DropletIdActionsBody
 * @version 2.0
 */
export class DropletIdActionsBody {
    /**
     * Constructs a <code>DropletIdActionsBody</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DropletIdActionsBody} obj Optional instance to populate.
     * @return {module:model/DropletIdActionsBody} The populated <code>DropletIdActionsBody</code> instance.
     */
    static constructFromObject(data: any, obj: any): any;
}
